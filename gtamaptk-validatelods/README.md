# gtamaptk-validatelods
This tool validates LOD assignments to verify whether or not they point to a valid model.

## Usage
```
gtamaptk-validatelods [input IDE] [input text IPL] [input binary IPL]
```